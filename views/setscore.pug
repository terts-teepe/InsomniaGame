extends layout

block content
  div(class="container")
    table
      thead
        tr: th Username
          th Score
      tbody
        each score in scores
          tr: td #{score.userId}
            td #{score.score}